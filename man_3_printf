.TH _printf
.SH NAME.
_printf \- prints depending on the type of format
.SH SYNOPSIS.
.B #include \(dqmain.h\(dq
.sp
.B int _printf(const char * format
.B , ...);
.
.SH DESCRIPTION.
.B The function _printf is a variadic function that produces a string according to a format and prints it to the standard output, much like the C standard library function printf. It can receive a variable number of arguments.

.sp
.B Return value
.sp
On success, _printf returns the number of characters printed. If
.I format
is NULL or if the functions encounters any errors, it returns -1.
.sp
.B Format of the format string
.sp
The format string can contain any characters, just like a regular string, in double quotes, which will be printed as is. For example, _printf(\(dqHello\(dq) will print: Hello.
However, this format string can contain.

.SH OPTIONS.
.B  Each format specification begins with a percentage sign and ends with a letter. These cases are in _printf.
.sp
.B  %c	print a character.

.B  %s	print a string.
.sp
.B  %%	print a single.

.SH EXAMPLE.
to print Mother's Day

#include "main.h"

int month;

month = 8;

*char = "march";

printf ("today is %d from %s, Mother's Day");

.SH AUTHORS.
Fernando Gonzales Pradinett (https://github.com/gpradinett) & Juan Carlos Garay (https://github.com/juan20carlos)


